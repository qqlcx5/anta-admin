# AI 研发规范

## 核心原则

1. **最高纲领**：宁可推倒重写，不可修修补补；宁可破坏旧用法，不可制造“双轨制”或并存两套心智模型。
2. **不为历史兜底，但必须先告知**：本项目是通用脚手架，默认建立统一极简体系。凡未明确要求兼容的，一律不为历史代码兜底（严禁编写兼容垫片、保留旧别名或支持双重数据格式），但每次行动前必须明确告知我。
3. **单一心智模型**：同一功能仅允许存在一种标准实现，彻底剥离冗余（如统一采用 `AtIcon` 与 `useRenderIcon`，统一采用 `anta-` 标识）。

## 对抗式审查（提交前必查）

1. **安全防线**：涉及原生 SVG/HTML 渲染时，必须过滤 `<script>`、`<foreignObject>`、内联 `on*` 事件及 `javascript:` 伪协议。
2. **运行时健壮性**：
   - 缓存仅使用纯字符串原子标识，严禁对包含函数或复杂引用的对象执行 `JSON.stringify`。
   - 样式合并使用数组格式（如 `:style="[base, custom]"`），严禁用 `Object.assign` 破坏字符串样式。
   - 针对 `null`、`undefined`、空串及异常结构做严格防御。
3. **彻底清理**：切除旧代码时，连带清理无用的静态资源（字体、CSS、打包规则等）。
